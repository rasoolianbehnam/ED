int scan(char* cmdLine, char* head, char* tail) {
    char* token;
    char s[] = '|';
    char tmp[strlen(cmdLine)];
    strcpy(tmp, cmdLine);
    token = strtok(cmdLine, s);
    while (token) {
        prin
    }
}
